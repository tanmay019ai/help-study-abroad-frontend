"use client";

import { Button, Typography } from "@mui/material";

export default function Home() {
  return (
    <div style={{ padding: 24 }}>
      <Typography variant="h4">Help Study Abroad</Typography>
      <Button variant="contained" sx={{ mt: 2 }}>
        MUI Working
      </Button>
    </div>
  );
}
